/*
 * Agent / Assessor Wallet Service
 * Placeholder only.
 * Future responsibilities:
 * - read wallet balance via RLS-safe queries
 * - read transaction ledger
 * - request payout via controlled RPC only
 * - never insert/update ledger directly from frontend
 */

export const WalletService = {
  status: 'placeholder'
};
