/*
 * PlayPro Verification Seal Service
 * Placeholder only.
 * Future responsibilities:
 * - read public verification seal
 * - read organization verification status
 * - submit verification request via controlled RPC
 */

export const VerificationService = {
  status: 'placeholder'
};
